# Emerge Provider Data - Gentoo Linux package manager
version: "1.0"

provider:
  name: "emerge"
  display_name: "Portage Emerge"
  description: "Source-based package manager for Gentoo Linux"
  type: "package_manager"
  platforms: ["gentoo"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via Emerge"
    steps:
      - name: "sync-portage"
        command: "emerge --sync"
        timeout: 300
      - name: "install-packages"
        command: "emerge {{sai_packages(saidata, 'emerge')}}"
        timeout: 3600
    validation:
      command: "qlist -I | grep {{saidata.metadata.name}}"
      expected_exit_code: 0
    rollback: "emerge --unmerge {{sai_packages(saidata, 'emerge')}}"

  uninstall:
    description: "Remove packages via Emerge"
    template: "emerge --unmerge {{sai_packages(saidata, 'emerge')}}"
    validation:
      command: "! qlist -I | grep {{saidata.metadata.name}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via Emerge"
    steps:
      - name: "sync-portage"
        command: "emerge --sync"
        timeout: 300
      - name: "upgrade-packages"
        command: "emerge -u {{sai_packages(saidata, 'emerge')}}"
        timeout: 3600

  start:
    description: "Start service via rc-service"
    template: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} start"
    validation:
      command: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} status"
      expected_output: "started"

  stop:
    description: "Stop service via rc-service"
    template: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} stop"
    validation:
      command: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} status"
      expected_output: "stopped"

  restart:
    description: "Restart service via rc-service"
    template: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} restart"

  enable:
    description: "Enable service auto-start"
    template: "rc-update add {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} default"

  disable:
    description: "Disable service auto-start"
    template: "rc-update del {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} default"

  status:
    description: "Check service status"
    template: "rc-service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'emerge')}} status"

  logs:
    description: "Show service logs"
    template: "tail -n 50 {{sai_file(saidata) or '/var/log/' + saidata.metadata.name + '/' + saidata.metadata.name + '.log'}}"

  info:
    description: "Show package information"
    template: "emerge --info {{sai_package(saidata, 0, 'emerge')}}"

  search:
    description: "Search for packages"
    template: "emerge --search {{saidata.metadata.name}}"

  list:
    description: "List installed packages"
    template: "qlist -I | grep {{saidata.metadata.name}}"
