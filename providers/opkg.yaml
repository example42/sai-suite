# OPKG Provider Data - OpenWrt package manager
version: "1.0"

provider:
  name: "opkg"
  display_name: "Open Package Management"
  description: "Lightweight package manager for OpenWrt and embedded systems"
  type: "package_manager"
  platforms: ["openwrt", "embedded"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via OPKG"
    steps:
      - name: "update-package-list"
        command: "opkg update"
      - name: "install-packages"
        command: "opkg install {{sai_packages(saidata, 'opkg')}}"
    timeout: 300
    validation:
      command: "opkg list-installed | grep {{saidata.metadata.name}}"
      expected_exit_code: 0
    rollback: "opkg remove {{sai_packages(saidata, 'opkg')}}"

  uninstall:
    description: "Remove packages via OPKG"
    template: "opkg remove {{sai_packages(saidata, 'opkg')}}"
    validation:
      command: "! opkg list-installed | grep {{saidata.metadata.name}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via OPKG"
    steps:
      - name: "update-package-list"
        command: "opkg update"
      - name: "upgrade-packages"
        command: "opkg upgrade {{sai_packages(saidata, 'opkg')}}"
    timeout: 300

  start:
    description: "Start service via init script"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} start"
    validation:
      command: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} status"
      expected_exit_code: 0

  stop:
    description: "Stop service via init script"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} stop"

  restart:
    description: "Restart service via init script"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} restart"

  enable:
    description: "Enable service auto-start"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} enable"

  disable:
    description: "Disable service auto-start"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} disable"

  status:
    description: "Check service status"
    template: "/etc/init.d/{{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}} status"

  logs:
    description: "Show service logs"
    template: "logread | grep {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'opkg')}}"

  info:
    description: "Show package information"
    template: "opkg info {{sai_package(saidata, 0, 'opkg')}}"

  search:
    description: "Search for packages"
    template: "opkg find {{saidata.metadata.name}}"

  list:
    description: "List installed packages"
    template: "opkg list-installed | grep {{saidata.metadata.name}}"
