# NuGet Provider Data - .NET package manager
version: "1.0"

provider:
  name: "nuget"
  display_name: "NuGet"
  description: "Package manager for .NET"
  type: "package_manager"
  platforms: ["linux", "macos", "windows"]
  executable: "dotnet"  # Main executable for availability detection
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "version", "start", "stop", "restart", "status"]

actions:
  install:
    description: "Install packages via NuGet"
    template: "dotnet tool install -g {{sai_packages(saidata, 'nuget')}}"
    timeout: 300
    detection: "nuget list {{sai_package(saidata, 'nuget')}} >/dev/null 2>&1"
    validation:
      command: "dotnet tool list -g | grep {{sai_package(saidata, 'nuget')}}"
      expected_exit_code: 0
    rollback: "dotnet tool uninstall -g {{sai_packages(saidata, 'nuget')}}"

  uninstall:
    description: "Remove packages via NuGet"
    template: "dotnet tool uninstall -g {{sai_packages(saidata, 'nuget')}}"
    detection: "nuget list {{sai_package(saidata, 'nuget')}} >/dev/null 2>&1"
    validation:
      command: "! dotnet tool list -g | grep {{sai_package(saidata, 'nuget')}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via NuGet"
    template: "dotnet tool update -g {{sai_packages(saidata, 'nuget')}}"
    timeout: 300
    detection: "nuget list {{sai_package(saidata, 'nuget')}} >/dev/null 2>&1"

  start:
    description: "Start .NET application"
    template: "{{sai_command(saidata, 'default')}}"

  stop:
    description: "Stop .NET application"
    template: "pkill -f {{sai_package(saidata, 'nuget')}}"

  restart:
    description: "Restart .NET application"
    steps:
      - name: "stop-app"
        command: "pkill -f {{sai_package(saidata, 'nuget')}}"
        ignore_failure: true
      - name: "start-app"
        command: "{{sai_command(saidata, 'default')}}"

  status:
    description: "Check application status"
    template: "pgrep -f {{sai_package(saidata, 'nuget')}}"

  info:
    description: "Show package information"
    template: "dotnet tool list -g | grep {{sai_package(saidata, 'nuget')}}"

  search:
    description: "Search for packages"
    template: "dotnet package search {{sai_package(saidata, 'nuget')}}"

  list:
    description: "List installed packages"
    template: "dotnet tool list -g | grep {{sai_package(saidata, 'nuget')}}"

  version:
    description: "Show package version"
    template: "dotnet tool list -g | grep '^{{sai_package(saidata, 'nuget')}}'"
