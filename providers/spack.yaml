# Spack Provider Data - Scientific computing package manager
version: "1.0"

provider:
  name: "spack"
  display_name: "Spack"
  description: "A flexible package manager for scientific computing"
  type: "package_manager"
  platforms: ["linux", "macos"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "status"]

actions:
  install:
    description: "Install packages via Spack"
    template: "spack install {{sai_packages(saidata, 'spack')}}"
    timeout: 3600
    validation:
      command: "spack find | grep {{saidata.metadata.name}}"
      expected_exit_code: 0
    rollback: "spack uninstall -y {{sai_packages(saidata, 'spack')}}"

  uninstall:
    description: "Remove packages via Spack"
    template: "spack uninstall -y {{sai_packages(saidata, 'spack')}}"
    validation:
      command: "! spack find | grep {{saidata.metadata.name}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via Spack"
    steps:
      - name: "uninstall-old"
        command: "spack uninstall -y {{sai_packages(saidata, 'spack')}}"
        ignore_failure: true
      - name: "install-new"
        command: "spack install {{sai_packages(saidata, 'spack')}}"
    timeout: 3600

  start:
    description: "Start application"
    template: "spack load {{sai_package(saidata, 0, 'spack')}} && {{sai_command(saidata, 'default')}}"

  stop:
    description: "Stop application"
    template: "pkill -f {{saidata.metadata.name}}"

  restart:
    description: "Restart application"
    steps:
      - name: "stop-app"
        command: "pkill -f {{saidata.metadata.name}}"
        ignore_failure: true
      - name: "start-app"
        command: "spack load {{sai_package(saidata, 0, 'spack')}} && {{sai_command(saidata, 'default')}}"

  status:
    description: "Check application status"
    template: "pgrep -f {{saidata.metadata.name}}"

  info:
    description: "Show package information"
    template: "spack info {{sai_package(saidata, 0, 'spack')}}"

  search:
    description: "Search for packages"
    template: "spack list | grep {{saidata.metadata.name}}"

  list:
    description: "List installed packages"
    template: "spack find | grep {{saidata.metadata.name}}"
