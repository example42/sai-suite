# Scoop Provider Data - Windows package manager
version: "1.0"

provider:
  name: "scoop"
  display_name: "Scoop"
  description: "A command-line installer for Windows"
  type: "package_manager"
  platforms: ["windows"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "status"]

actions:
  install:
    description: "Install packages via Scoop"
    template: "scoop install {{sai_packages(saidata, 'scoop')}}"
    timeout: 600
    detection: "scoop info {{sai_package(saidata, 'scoop')}} >/dev/null 2>&1"
    validation:
      command: "scoop list | findstr {{sai_package(saidata, 'scoop')}}"
      expected_exit_code: 0
    rollback: "scoop uninstall {{sai_packages(saidata, 'scoop')}}"

  uninstall:
    description: "Remove packages via Scoop"
    template: "scoop uninstall {{sai_packages(saidata, 'scoop')}}"
    detection: "scoop info {{sai_package(saidata, 'scoop')}} >/dev/null 2>&1"
    validation:
      command: "scoop list | findstr {{sai_package(saidata, 'scoop')}} || exit 1"
      expected_exit_code: 1

  upgrade:
    description: "Upgrade packages via Scoop"
    template: "scoop update {{sai_packages(saidata, 'scoop')}}"
    timeout: 600
    detection: "scoop info {{sai_package(saidata, 'scoop')}} >/dev/null 2>&1"

  start:
    description: "Start application"
    template: "{{sai_command(saidata, 'default')}}"

  stop:
    description: "Stop application"
    template: "taskkill /F /IM {{sai_package(saidata, 'scoop')}}.exe"

  restart:
    description: "Restart application"
    steps:
      - name: "stop-app"
        command: "taskkill /F /IM {{sai_package(saidata, 'scoop')}}.exe"
        ignore_failure: true
      - name: "start-app"
        command: "{{sai_command(saidata, 'default')}}"

  status:
    description: "Check application status"
    template: "tasklist | findstr {{sai_package(saidata, 'scoop')}}.exe"

  info:
    description: "Show package information"
    template: "scoop info {{sai_package(saidata, 'scoop')}}"

  search:
    description: "Search for packages"
    template: "scoop search {{sai_package(saidata, 'scoop')}}"

  list:
    description: "List installed packages"
    template: "scoop list | findstr {{sai_package(saidata, 'scoop')}}"
