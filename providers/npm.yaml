# NPM Provider Data - Node.js package manager
version: "1.0"

provider:
  name: "npm"
  display_name: "Node Package Manager"
  description: "Package manager for Node.js"
  type: "package_manager"
  platforms: ["linux", "macos", "windows"]
  executable: "npm"  # Main executable for availability detection
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "version", "start", "stop", "restart", "status"]

actions:
  install:
    description: "Install packages via NPM"
    template: "npm install -g {{sai_packages(saidata, 'npm')}}"
    timeout: 300
    detection: "npm view {{sai_package(saidata, 'npm')}} >/dev/null 2>&1"
    validation:
      command: "npm list -g | grep {{sai_package(saidata, 'npm')}}"
      expected_exit_code: 0
    rollback: "npm uninstall -g {{sai_packages(saidata, 'npm')}}"

  uninstall:
    description: "Remove packages via NPM"
    template: "npm uninstall -g {{sai_packages(saidata, 'npm')}}"
    detection: "npm list -g | grep -q {{sai_package(saidata, 'npm')}}"
    validation:
      command: "! npm list -g | grep {{sai_package(saidata, 'npm')}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via NPM"
    template: "npm update -g {{sai_packages(saidata, 'npm')}}"
    timeout: 300
    detection: "npm list -g | grep -q {{sai_package(saidata, 'npm')}}"

  start:
    description: "Start Node.js application"
    template: "{{sai_command(saidata, 'default')}}"

  stop:
    description: "Stop Node.js application"
    template: "pkill -f {{sai_package(saidata, 'npm')}}"

  restart:
    description: "Restart Node.js application"
    steps:
      - name: "stop-app"
        command: "pkill -f {{sai_package(saidata, 'npm')}}"
        ignore_failure: true
      - name: "start-app"
        command: "{{sai_command(saidata, 'default')}}"

  status:
    description: "Check application status"
    template: "pgrep -f {{sai_package(saidata, 'npm')}}"

  info:
    description: "Show package information"
    template: "npm info {{sai_package(saidata, 'npm')}}"

  search:
    description: "Search for packages"
    template: "npm search {{sai_package(saidata, 'npm')}}"

  list:
    description: "List installed packages"
    template: "npm list -g | grep {{sai_package(saidata, 'npm')}}"

  version:
    description: "Show package version"
    template: "npm list -g {{sai_package(saidata, 'npm')}} --depth=0"
