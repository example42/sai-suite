# YUM Provider Data - Legacy RHEL/CentOS package manager
version: "1.0"

provider:
  name: "yum"
  display_name: "Yellowdog Updater Modified"
  description: "Legacy package manager for RPM-based systems"
  type: "package_manager"
  platforms: ["rhel", "centos", "scientific"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via YUM"
    template: "yum install -y {{sai_packages(saidata, 'yum')}}"
    timeout: 600
    validation:
      command: "rpm -qa | grep {{saidata.metadata.name}}"
      expected_exit_code: 0
    rollback: "yum remove -y {{sai_packages(saidata, 'yum')}}"

  uninstall:
    description: "Remove packages via YUM"
    template: "yum remove -y {{sai_packages(saidata, 'yum')}}"
    validation:
      command: "! rpm -qa | grep {{saidata.metadata.name}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via YUM"
    template: "yum update -y {{sai_packages(saidata, 'yum')}}"
    timeout: 600

  start:
    description: "Start service via service command"
    template: "service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} start"
    validation:
      command: "service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} status"
      expected_exit_code: 0

  stop:
    description: "Stop service via service command"
    template: "service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} stop"

  restart:
    description: "Restart service via service command"
    template: "service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} restart"

  enable:
    description: "Enable service auto-start"
    template: "chkconfig {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} on"

  disable:
    description: "Disable service auto-start"
    template: "chkconfig {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} off"

  status:
    description: "Check service status"
    template: "service {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'yum')}} status"

  logs:
    description: "Show service logs"
    template: "tail -n 50 {{sai_file(saidata) or '/var/log/' + saidata.metadata.name + '/' + saidata.metadata.name + '.log'}}"

  info:
    description: "Show package information"
    template: "yum info {{sai_package(saidata, 0, 'yum')}}"

  search:
    description: "Search for packages"
    template: "yum search {{saidata.metadata.name}}"

  list:
    description: "List installed packages"
    template: "rpm -qa | grep {{saidata.metadata.name}}"
