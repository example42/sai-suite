# Pacman Provider Data - Arch Linux package manager
version: "1.0"

provider:
  name: "pacman"
  display_name: "Pacman Package Manager"
  description: "Package manager for Arch Linux and derivatives"
  type: "package_manager"
  platforms: ["arch", "manjaro", "endeavouros"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via Pacman"
    template: "pacman -S --noconfirm {{sai_packages(saidata, 'pacman')}}"
    timeout: 600
    validation:
      command: "pacman -Q | grep {{saidata.metadata.name}}"
      expected_exit_code: 0
    rollback: "pacman -R --noconfirm {{sai_packages(saidata, 'pacman')}}"

  uninstall:
    description: "Remove packages via Pacman"
    template: "pacman -R --noconfirm {{sai_packages(saidata, 'pacman')}}"
    validation:
      command: "! pacman -Q | grep {{saidata.metadata.name}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via Pacman"
    steps:
      - name: "sync-database"
        command: "pacman -Sy"
      - name: "upgrade-packages"
        command: "pacman -S --noconfirm {{sai_packages(saidata, 'pacman')}}"
    timeout: 600

  start:
    description: "Start service via systemctl"
    template: "systemctl start {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"
    validation:
      command: "systemctl is-active {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"
      expected_output: "active"

  stop:
    description: "Stop service via systemctl"
    template: "systemctl stop {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"
    validation:
      command: "systemctl is-active {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"
      expected_output: "inactive"

  restart:
    description: "Restart service via systemctl"
    template: "systemctl restart {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"

  enable:
    description: "Enable service auto-start"
    template: "systemctl enable {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"

  disable:
    description: "Disable service auto-start"
    template: "systemctl disable {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"

  status:
    description: "Check service status"
    template: "systemctl status {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}}"

  logs:
    description: "Show service logs"
    template: "journalctl -u {{sai_service(saidata, 0, 'service_name') or sai_package(saidata, 0, 'pacman')}} --no-pager -n 50"

  info:
    description: "Show package information"
    template: "pacman -Si {{sai_package(saidata, 0, 'pacman')}}"

  search:
    description: "Search for packages"
    template: "pacman -Ss {{saidata.metadata.name}}"

  list:
    description: "List installed packages"
    template: "pacman -Q | grep {{saidata.metadata.name}}"
