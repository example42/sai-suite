# PKG Provider Data - FreeBSD package manager
version: "1.0"

provider:
  name: "pkg"
  display_name: "FreeBSD Package Manager"
  description: "Binary package manager for FreeBSD and DragonFly BSD"
  type: "package_manager"
  platforms: ["freebsd", "dragonfly"]
  capabilities: ["install", "uninstall", "upgrade", "search", "info", "list", "start", "stop", "restart", "enable", "disable", "status", "logs"]

actions:
  install:
    description: "Install packages via PKG"
    template: "pkg install -y {{sai_packages(saidata, 'pkg')}}"
    timeout: 600
    detection: "pkg info {{sai_package(saidata, 'pkg')}} >/dev/null 2>&1"
    validation:
      command: "pkg info | grep {{sai_package(saidata, 'pkg')}}"
      expected_exit_code: 0
    rollback: "pkg delete -y {{sai_packages(saidata, 'pkg')}}"

  uninstall:
    description: "Remove packages via PKG"
    template: "pkg delete -y {{sai_packages(saidata, 'pkg')}}"
    detection: "pkg info {{sai_package(saidata, 'pkg')}} >/dev/null 2>&1"
    validation:
      command: "! pkg info | grep {{sai_package(saidata, 'pkg')}}"
      expected_exit_code: 0

  upgrade:
    description: "Upgrade packages via PKG"
    template: "pkg upgrade -y {{sai_packages(saidata, 'pkg')}}"
    timeout: 600
    detection: "pkg info {{sai_package(saidata, 'pkg')}} >/dev/null 2>&1"

  start:
    description: "Start service via service command"
    template: "service {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}} start"
    validation:
      command: "service {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}} status"
      expected_exit_code: 0

  stop:
    description: "Stop service via service command"
    template: "service {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}} stop"

  restart:
    description: "Restart service via service command"
    template: "service {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}} restart"

  enable:
    description: "Enable service auto-start"
    template: "sysrc {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}}_enable=YES"

  disable:
    description: "Disable service auto-start"
    template: "sysrc {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}}_enable=NO"

  status:
    description: "Check service status"
    template: "service {{sai_service(saidata, 'service_name') or sai_package(saidata, 'pkg')}} status"

  logs:
    description: "Show service logs"
    template: "tail -n 50 {{sai_file(saidata) or '/var/log/' + saidata.metadata.name + '/' + saidata.metadata.name + '.log'}}"

  info:
    description: "Show package information"
    template: "pkg info {{sai_package(saidata, 'pkg')}}"

  search:
    description: "Search for packages"
    template: "pkg search {{sai_package(saidata, 'pkg')}}"

  list:
    description: "List installed packages"
    template: "pkg info | grep {{sai_package(saidata, 'pkg')}}"
